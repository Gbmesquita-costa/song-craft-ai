const publicRoutes = ["/"];
const apiAuthPrefix = "/api/auth";

const authRoutes = ["/signin", "/signup", "/forgot-password"];
const DEFAULT_SIGNIN_REDIRECT = "/dashboard";

export { publicRoutes, authRoutes, apiAuthPrefix, DEFAULT_SIGNIN_REDIRECT };
